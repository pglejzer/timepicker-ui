# Dependencies

### Zero Runtime Dependencies
The library ships zero runtime dependencies — do not add any. Dev/build dependencies are acceptable but must be justified. SSR support and a small bundle depend on this. Sources: docs, config (README, architecture.md, build-release agent; package.json has no runtime deps).
